apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  type: NodePort
  selector:
    app: blog-app
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: {{ .Values.app.port | int }}
      nodePort: {{ .Values.service.nodePort | int }}